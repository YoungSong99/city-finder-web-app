<ul class="cityList">
  <% cities.each do |city| %>
    <li class="cityItem" id="<%= dom_id(city) %>_item">
      <span class="emoji">🇺🇸</span>
      <h3 class="name"><%= link_to city.city_name, priority_result_detail_path(city) %></h3>
      <div id="<%= dom_id(city) %>_btn">
        <% if user_signed_in? %>
          <% if current_user.favorite_cities.exists?(city_id: city.id) %>
            <%= button_to '-', remove_city_path(city), method: :delete, remote: true, data: { turbo: false }, class: 'deleteBtn custom-tooltip remove' %>
          <% else %>
            <%= button_to '+', add_city_path(city), method: :post, remote: true, data: { turbo: false }, class: 'deleteBtn custom-tooltip add' %>
          <% end %>
          <!-- ^ Your ajax code isn't working well here -->
        <% else %>
          <%= button_to '+', add_city_path(city), method: :post, remote: true, data: { turbo: false }, class: 'deleteBtn custom-tooltip add' %>
        <% end %>
      </div>
    </li>
  <% end %>
</ul>
