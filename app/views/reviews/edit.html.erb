<div class="app">
  <div class="sidebar">
    <img src="<%= asset_path('logo.png') %>" alt="CityFinder logo" class="logo">
    <%= render 'shared/nav' %>
    <div class="outlet">
      <h1 class="sub-title">💡Edit Review for <%= @review.city.city_name %></h1>
      <%= render 'form', city: @review.city, review: @review %>
    </div>
    <%= render 'shared/footer' %>
  </div>
  <%= render 'shared/mapbox_city', city:@city %>
  <%= render 'shared/user_nav' %>
  <%= render 'shared/nav_mobile' %>
  <div class="side_mobile_city">
    <%= render 'form', city: @review.city, review: @review %>
  </div>
</div>