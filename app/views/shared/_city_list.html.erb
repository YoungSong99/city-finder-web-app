<ul class="cityList">
  <% cities.each do |city| %>
    <li class="cityItem" id="<%= dom_id(city) %>_item">
      <span class="emoji">🇺🇸</span>
      <h3 class="name"><%= link_to city.city_name, city_detail_path(city) %></h3>
      <div id="<%= dom_id(city) %>_btn">
        <% if user_signed_in? %>
          <% if current_user.favorite_cities.exists?(city_id: city.id) %>
            <%= button_to '-', favorite_city_path(id: city.id), method: :delete, remote: true, data: { turbo: false }, class: 'deleteBtn custom-tooltip remove' %>
          <% else %>
            <%= button_to '+', favorite_cities_path(id: city.id), method: :post, remote: true, data: { turbo: false }, class: 'deleteBtn custom-tooltip add' %>
          <% end %>
        <% else %>
          <%= button_to '+', favorite_cities_path(id: city.id), method: :post, remote: true, data: { turbo: false }, class: 'deleteBtn custom-tooltip add' %>
        <% end %>
      </div>
    </li>
  <% end %>

</ul>