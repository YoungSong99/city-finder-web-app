<nav class="static-nav">
  <%= render 'shared/logo' %>
  <ul>
    <li>
      <%= link_to 'How to use', about_path, class: ('active' if current_page?(about_path)) %>
    </li>
    <li>
      <%= link_to 'Find your city', find_path(:filter), class: ('active' if current_page?(find_path(:filter))) %>
    </li>
    <li>
      <%= link_to 'Contact', contact_path, class: ('active' if current_page?(contact_path)) %>
    </li>
    <li>
      <% if user_signed_in? %>
        <%= link_to 'Log Out', destroy_user_session_path, data: { turbo_method: :delete }, class: "ctaLink"%>
      <% else %>
        <%= link_to 'Log In', new_user_session_path, class: "ctaLink" %>
      <% end %>
    </li>
  </ul>
</nav>