<main class="about">
  <%= render 'shared/static_nav' %>
  <section>
    <div>
      <h2>
        I'd love to
        <br/>
        Hear from you!</i>
      </h2>
      <p>
        Whether it’s good news, constructive feedback, or just a quick hello, I’m all ears! Your input helps make CityFinder better for everyone.
      </p>
    </div>
    <div class="form-container form">
      <form id="contactForm" class="form">
        <div class="row">
          <label for="category">How can I help you?</label>
          <select id="category" name="category" onchange="updatePlaceholder()" class="select">
            <option value="bug">I found a bug 🐜</option>
            <option value="suggestion">I have a suggestion 💡</option>
            <option value="incorrect_data">I found incorrect data 📊</option>
            <option value="say_hi">I just want to say hi 👋</option>
          </select>
        </div>
        <div class="row">
          <label for="message">Your Message</label>
          <textarea id="message" name="message" rows="6" placeholder="Please enter your message here..."></textarea>
        </div>
        <div class="row">
          <label for="email">Your Email</label>
          <input type="email" id="email" name="email" placeholder="Your email address..." required>
        </div>
        <button type="submit" class="btn primary large">Send Message</button>
      </form>
    </div>
  </section>
</main>
<%= render 'shared/footer' %>
<script>
    function updatePlaceholder() {
        const category = document.getElementById('category').value;
        const messageField = document.getElementById('message');

        switch (category) {
            case 'bug':
                messageField.placeholder = "Oops! Let me know what went wrong. I’ll fix it soon.";
                break;
            case 'suggestion':
                messageField.placeholder = "Got an idea to make CityFinder even better? I’d love to hear it!";
                break;
            case 'incorrect_data':
                messageField.placeholder = "If something doesn’t look right, please tell me so I can make it accurate.";
                break;
            case 'say_hi':
                messageField.placeholder = "Don’t be shy! I’m always happy to hear from you, even if it’s just a friendly hello.";
                break;
            default:
                messageField.placeholder = "Please enter your message here...";
        }
    }

    document.addEventListener('DOMContentLoaded', updatePlaceholder);
</script>
