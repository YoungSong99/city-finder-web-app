<ul class="favoriteCityList">
  <% favorite_cities.each do |favorite_city| %>
    <li class="cityItem" id="<%= dom_id(favorite_city.city) %>_item">
      <span class="emoji">🇺🇸</span>
      <h3 class="name"><%= link_to favorite_city.city.city_name, comparison_result_detail_path(favorite_city.city) %></h3>
      <div id="<%= dom_id(favorite_city.city) %>_btn">
        <%= button_to '-', remove_city_path(favorite_city.city), method: :delete, remote: true, data: { turbo: false }, class: 'deleteBtn' %>
      </div>
    </li>
  <% end %>
</ul>