<div class="user">
  <span> <span class="emoji">🙌</span>Welcome,
    <% if user_signed_in? %>
      <%= link_to current_user.username.capitalize, edit_user_registration_path, class: "user-edit" %></span>
      <%= link_to 'Log Out', destroy_user_session_path, method: :delete, data: { turbo: false, confirm: 'Are you sure?' }, class: 'logout-btn' %>
    <% else %>
      Guest</span>
      <%= link_to 'Log In', new_user_session_path, class: 'logout-btn' %>
    <% end %>
    </div>